apiVersion: v1
kind: Service
metadata:
  name: voms-rest
  labels:
    customer: kubernetes_topup
    product: topup
    owner: mohamed_elbahrawy
    purpose: rnd        
spec:
    selector:
        app: voms-rest
    ports:      
      - name: port8080
        port: 8080    

